const UI_SELECT = (props) => {
    var THIS = {}

    THIS.IS = HERE.IS

    THIS.MODULES = HERE.IS.MODULES.INNIT

    THIS.Contructor = () => {
        return {
            data: props.data,
            changeEvent: props.changeEvent
        }
    }

    THIS.Element = () => {

        const html = (
            `
            <div class="sete2otyzotu2odewota">
                THIS IS SELECT BOX
            </div>

            `

        )
        return html
    }

    return THIS
}

UI_SELECT