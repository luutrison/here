const THIS_SETTING = {
    USE: HERE.IS
}

const PROPS = {


}

const THIS = {}

const SETTING = THIS.SETTING = THIS_SETTING

const IS = SETTING.USE

const CREATE = THIS.CREATE = ({props}) => {

    const element = ({props}) => {

        const elementProps = {
            changeMethod: props.changeMethod,
            arrContent: props.arrContent
        
        }

        return (`
           <div>


            </div>
        
        `)
    }
}

THIS
